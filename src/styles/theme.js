import { breakpoints } from './breakpoints';
import { fontSize } from './font-size';
import { rem } from './rem';
import { space } from './space';

const theme = {
  breakpoints,
  fontSize,
  rem,
  space,
};

export { theme };